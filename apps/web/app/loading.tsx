import { PageLoadingState } from "@/components/page-loading-state";

export default function Loading() {
  return (
    <PageLoadingState
      title="Loading Enterprise IDP"
      description="Preparing the shared frontend scaffold."
    />
  );
}
