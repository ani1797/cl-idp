{
  "name": "@enterprise-idp/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared generated API types for Enterprise IDP.",
  "type": "module",
  "scripts": {
    "generate": "mkdir -p src/generated && openapi-typescript ../../docs/spec/api/openapi.yaml -o src/generated/api.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.10.1"
  }
}
